/**
 * Football Market Baseline Postgame Review v0 — paths.
 * Research observation only. Not Official KPI.
 */
export function footballMarketBaselineReviewV0Rel(dateKst: string): string {
  return `data/research/football/${dateKst}-market-baseline-review-v0.json`;
}

export function footballMarketBaselineScorecardV0Rel(dateKst: string): string {
  return `data/research/football/${dateKst}-market-baseline-scorecard-v0.json`;
}
