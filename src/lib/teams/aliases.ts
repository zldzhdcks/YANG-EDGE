/**
 * @deprecated 새 코드는 `@/lib/teams` 를 사용한다.
 * 기존 import 경로 호환용 재수출.
 */
export {
  getDisplayTeamName,
  getDisplayMatchLabel,
  getTeamDisplayName,
  getMatchDisplayLabel,
} from "./get-team-display-name";
export { normalizeTeamName } from "./normalize-team-name";
export { TEAM_ALIASES } from "./team-aliases";
