# External league execution V1 — audit / Cursor handoff

Entry gate: branch and HEAD/upstream matched `f7cd4fc5d7b809536173add181230f7b9a86e094`; working tree was clean before creating the authorized runner/tests. Cross-protocol canonical SHA256 matched `6bedb86389f62afae5feebdd098b9ab6d4b2c151719ef267309d562f8df3e31f`. The execution CLI requires that existing tracked files remain unchanged and only these authorized new files are present. No preliminary runner commit changes the expected base.

The new wrapper imports the frozen EPL `predictOne`, history selector, metric scorer and classification functions without editing them. It replaces only their EPL-specific archive/protocol provenance identifiers before prediction persistence. Each league supplies its own regular history; no other league's result is an input. Scope follows the sealed metadata-only predicate. All four Bundesliga playoffs stay in raw archive/quarantine and are excluded from context and targets, regardless of status or result. In-scope identity/score/round-robin contracts are checked before execution.

Predictions for an entire league are fsynced and read back with an exact-byte hash before labels are joined. Same-kickoff histories are frozen copies. Three leagues execute in the prescribed order without printing intermediate performance or adjusting any state shared across leagues. Synthetic tests check reversed execution order. EPL is loaded as an existing sealed reference, never rerun.

The CLI verifies protocol/model/archive/scope hashes and the previous EPL implementation hashes; records execution source hashes before any external prediction; denies network entry points; and creates a permanent exclusive run directory plus per-league start receipts. Errors retain INVALID evidence and do not permit silent retry. Output is under the existing gitignored research-result root: `data/cache/research/football/poisson-chronological-backtest-v1/cross-league-v1/`. Do not delete it to rerun.

After tests, typecheck and lint pass, the separately authorized command is `tsx scripts/run-football-cross-league-backtest-v1.ts --execute-once`. After a valid run, **do not run that command again or modify runner/model/rules**. Read `data/audits/football-poisson-cross-league-backtest-v1.json` and `docs/FOOTBALL_POISSON_CROSS_LEAGUE_BACKTEST_V1.md`. The local full result has exact-byte SHA256 (UTF-8 including trailing newline); the tracked aggregate seal carries it and all independent league metrics, calibration, matrices and Q1–Q5 evidence. Execution source hashes normalize CRLF to LF.

Overall status is INVALID if any required league is invalid, ALL_BASELINES_MEASURED only when all four independent baseline classifications meet the unchanged floors, otherwise PARTIAL_BASELINE. No pooled accuracy or automatic PROMISING/VALIDATED_MODEL is produced. Q1 remains descriptive with no invented similarity threshold. Calibration differences are descriptive, not significance claims. All empty and small bins remain visible.

Original retrospective timestamps/48-hour assumed availability limitations remain. No actual observations are fabricated, no odds/provider predictions/owner private records are used, and no betting outputs are created. Git push after execution is administrative and outside the backtest's NETWORK_CALLS=0 assertion. Raw/per-match records remain local; only code, report and aggregate seal are pushed. STOP for CTO review; no V2, retuning or main merge.

Pre-prediction runtime correction: the first CLI launch failed during module transformation because a scope-audit CLI dependency used top-level await under a CJS transform. No run directory, prediction or performance result existed. The new CLI instead verifies the saved scope-audit hash and reconstructs pinned raw metadata decision hashes directly; frozen scope/model files are unchanged. Tests import the real CLI module graph to cover this compatibility issue. This is not a second statistical run or post-result tuning.

Pre-prediction identity check correction: a subsequent preflight found two provider display-name spellings for one stable Bundesliga team ID. The newly added display-name equality check was removed before any prediction; exact provider IDs remain authoritative as required by the archive contract. Names remain nonempty/valid but are never matched fuzzily or rewritten. Variants are recorded in the local result audit. No archive, model input identity, scope or temporal rule changed, and no execution-start directory existed at either preflight failure.
