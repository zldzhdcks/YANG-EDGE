# Football V4 Prospective Evaluation Policy V1

OUTCOME_BLIND_DESIGN_FROZEN=true. RATIFIED=false (independent pre-enrollment ratification remains outstanding). No collection or shadow launch is authorized by this document. The present mission requests a documented proposal; it does not supply independent ratification of a numerical sample/power policy. Parent: FOOTBALL_V4_RESEARCH_ADMISSION_PROTOCOL_V1.md, committed before this audit.

## Review checkpoints

Reuse the repository review convention N=25/50/100/200, counting distinct eligible paired graded fixtures per fixed candidate, with separate league counts. These are descriptive checkpoints, never automatic promotion, early stopping for success, or tuning opportunities. No samples or performance metrics were inspected to formulate this policy. This mission only reads prior governance statements, not the referenced result files.

## Final evaluation gate proposal

OFFICIAL_MIN_SAMPLE_FOUND=false. Proposed new floor: N>=100 in EACH league 39/140/135/78, total>=400. This is a NEW research proposal, not an established Official standard or power analysis. Keep at least 80% candidate coverage in each full authoritative league denominator, all classes represented, null required metrics => insufficient, and every original target/PASS/miss retained. Candidate denominator rules and feature missingness thresholds require explicit algorithm freeze before enrollment. Do not pool versions or count revisions twice.

Required comparisons against Official V1 use the exact frozen metric definitions and per-league/class guards from parent section 10. Passing them grants review eligibility only. Separate source rights, time/identity proof, immutable replay, operational safety and explicit owner/CTO promotion remain mandatory. AUTO_PROMOTION=false.

Before enrollment, an independent reviewer must ratify the above floors, coverage definition, grade completion delay, maximum collection duration, dependency-aware uncertainty method, and handling of the three-candidate family. These statistical details are NOT_DEFINED_FOR_EXECUTION here; no arbitrary statistical test is added. Their absence blocks launch, not this outcome-blind audit. No same-cohort repeated significance testing or winner selection after outcome inspection.

Enroll consecutive authoritative locked fixtures after all feature/candidate/source/parameter/cutoff freezes. Evaluate one complete slate prefix meeting all predeclared league count floors; pending/ungradable fixtures stay visible and follow the previously ratified completion policy. Intermediate operational checks cannot change this boundary. A new candidate version requires a new unseen evaluation cohort. Exposed 2025 data are not independent V4 holdout.

## Temporal clarification for this audit

The parent uses asOf as a proved input-availability upper bound: collectedAt <= asOf. The present request permits consistency with the existing contract. Retain that meaning, add observedAt <= collectedAt, require asOf < predictionCutoff (strict), and predictionCutoff <= scheduledStart minus 60 seconds. This implies collectedAt < cutoff too. Where asOf equals collectedAt it must be evidenced, not imputed. asOf is not provider publication time. Keep publication time separately and retain the parent's missing-publication blocker. No frozen document is rewritten.

The existing kickoff-only helper cannot prove this chain. Every source section needs its own receipt and bounds. Cache hits retain original receipt times. No file mtime substitutes for observation; no post-cutoff revision replaces an earlier artifact. No hindsight reconstruction of seasonal aggregates.

## Status

REVIEW_CHECKPOINTS=25,50,100,200
FINAL_EVALUATION_GATE=PROPOSED_PER_LEAGUE_100_TOTAL_400_PLUS_ALL_GUARDS
RATIFIED=false
AUTO_PROMOTION=false
PROSPECTIVE_ENROLLMENT_ALLOWED=false
V4_SHADOW_ALLOWED=false
SAMPLE_RESULTS_ACCESSED=false
